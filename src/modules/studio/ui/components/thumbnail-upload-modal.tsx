interface Props {
  videoId: string;
  open: boolean;
  onOpenChange: () => void;
}

export default function ThumbnailUploadModal({
  videoId,
  open,
  onOpenChange,
}: Props) {
  //   return <Responsi
}
